# 함께키즈 메인 백엔드 서비스 의존성

# FastAPI 및 웹 서버
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6

# 데이터베이스
sqlalchemy==2.0.23
alembic==1.13.1
PyMySQL==1.1.0  # MySQL 드라이버
cryptography==41.0.7  # PyMySQL 암호화 지원
# sqlite3는 Python 내장

# 인증 및 보안
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-decouple==3.8

# HTTP 클라이언트 (LLM 서비스 호출용)
httpx==0.25.2
requests==2.31.0

# 환경 변수 관리
python-dotenv==1.0.0

# 이미지 처리 (프로필 사진 등)
Pillow==10.1.0

# 날짜/시간 처리
python-dateutil==2.8.2

# 검증 및 직렬화
pydantic==2.5.0
email-validator==2.1.0

# 개발 및 테스트
pytest==7.4.3
pytest-asyncio==0.21.1
